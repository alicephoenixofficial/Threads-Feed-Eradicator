const url = document.URL.toString();

const path = "x1n2onr6";
let element13 = document.getElementsByClassName(path);

if (element13) {
    console.log("Element13: " + element13.length.toString());
    const child = element13[1];
    if (child) {
        const parent = child.parentNode;

        parent.removeChild(child);
    }
}
